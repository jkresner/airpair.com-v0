<div class="call">
  <a href="#{{_id}}" class="edit" title="edit call information" data-id="{{_id}}">
    edit
  </a>
  <img src="{{expert.pic}}?s=100" alt="{{expert.name}}">

  <a href="{{gcal.htmlLink}}" class="dateTime" title="view on google calendar" target="_blank">
    {{{callDateTime datetime}}}
  </a>

  <label>{{type}}</label>
  <label class="duration">
    {{duration}} {{inflect duration "hour" "hours"}}
  </label>

  <!-- <span class="label label-{{status}}">{{status}}</span> -->
  {{#if recordings}}
    {{#each recordings}}
    <a href="http://youtu.be/{{data.id}}" target="_blank">
      <img src="{{data.snippet.thumbnails.default.url}}"
        class="youtubeThumbnail" alt="youtube thumbnail">
    </a>
    {{/each}}
  {{else}}
    <div class="g-hangout" id="{{_id}}"></div>
    {{!-- <img src="/images/css/hangout.png" /></a> --}}
  {{/if}}
</div>
