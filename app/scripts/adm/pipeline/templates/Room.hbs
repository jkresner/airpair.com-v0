<div class="form-actions" style="margin-top:0">
  <div class="controls">
    <a href="#request/{{ rId }}" class="btn btn-inverse">Back</a>
    {{#if room._id}}
    <a href="#" class="btn btn-danger toggle" data-roomid="{{room._id}}">Disassociate</a>{{/if}}
  </div>
</div>

<div class="alert alert-success">
  <button type="button" class="close" data-dismiss="alert">&times;</button>
  <h4>Room save</h4>
</div>

<div class="alert alert-error">
  <button type="button" class="close" data-dismiss="alert">&times;</button>
  <h4>Error</h4>
</div>

{{#if roomJSON}}

<pre>{{roomJSON}}</pre>


{{#each members}}
  <br /><b>{{name}}</b> | @{{mention_name}}
{{/each}}

<br /><br />
<div class="control-group">
  <label class="control-label" for="reqOwner">Hello Msg</label>
  <div class="controls">
<textarea name="msg" style="width:400px;height:200px">
Hi guys, welcome to your private chat room :{}
1) HipChat is asynchronous. Offline users will see your messages as soon as they come back online. To send email notifications to offline users @ mention them like:{{#each members}}
@{{mention_name}}{{/each}}.
2) Now is the time clarify open questions about the request and if desired agree on (paid) prep time:
https://www.airpair.com/review/{{rId}}
3) Agree to and mention @all with the time and length you want to AirPair so we can schedule you and track hours without you worrying about it.
</textarea>
  </div>
</div>
<a href="#" class="send-msg btn btn-primary">Send</a>

{{else}}

<div class="new">

<h2>Room</h2>

<div class="control-group">
  <label class="control-label">Room name</label>
  <div class="controls">
    <input type="text" data-error-style="inline" value="{{ name }}" placeholder="Room name" name="roomName" class="input-block-level">
    <a href="#" class="btn-create btn disabled">Create</a>
    <div style="float:right">{{{tagsString}}}</div>
  </div>
</div>


<h2>Members</h2>

<div id="members"></div>

</div>
<div class="existing">
  <h2>Existing rooms</h2>

  <ul>
  {{#each rooms}}
    <li>{{name}} <a href="#" class="toggle associate" data-roomid="{{_id}}">associate</a></li>
  {{/each}}
  </ul>

</div>

{{/if}}

