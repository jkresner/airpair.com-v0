<td>
  <a class="delete" href="#" data-id="{{ _id }}">x</a>
</td>
<td><a href="#companys/{{ _id }}" class="edit" data-id="{{ _id }}"><b>{{ name }}</b></a></td>
<td>
  {{#if url}}
  <a href="http://{{ url }}}" target="_blank">{{url}}</a>
  {{else}}
   -
  {{/if}}
</td>
<td><div class="about">{{ about }}</div></td>
<td class="contacts">
  {{#each contacts }}
    <a href="mailto:{{email}}">{{ fullName }}</a>
  {{/each}}
</td>