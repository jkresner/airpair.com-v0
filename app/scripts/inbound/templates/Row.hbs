<td><a class="edit" href="#request/{{ _id }}" data-id="{{ _id }}">{{ status }}</a> {{ createdDate }}</td>
<td>
    <img src="{{ contactPic }}" />
    <a href="mailto:{{contactEmail}}" target="_blank">{{ contactName }}</a>
    {{ company.name }}
</td>
<td>
  {{#each tags}}
    <span class="label label-tags">{{short}}</span>
  {{/each}}
</td>
<td>{{ suggestedFitCount }} / {{ suggestedCount }}</td>
<td>{{ callCompleteCount }} / {{ callCount }}</td>
<td>-</td>