
<div class="expertMini suggestion">
  <div class="deets">

    <div class="fullName">
      {{ name }}
      {{#if username }}<br />@{{ username }}
    {{/if}}
    </div>
    <hr />

    <img src="{{ pic }}?s=40" class="pic" />

    <div class="links">
      {{> DevLinks }}
    </div>

  </div>

  <div class="tags">
    {{#each tags }}
      <span class="label label-tag">{{ short }}</span>
    {{/each }}
  </div>

  <a href="https://mail.google.com/mail/u/0/?shva=1#search/%22{{name}}%22+OR+{{email}}" class="email btn btn-small btn-inverse" target="_blank"><i class="icon-envelope icon-white"></i> </a>
  <a href="#" class="add-suggestion btn btn-small" data-id="{{ _id }}">Add (${{rate}})</a>
</div>
