<h2><b>{ </b>Request info<b> }</b></h2>

<div><label><b>Brief</b></label>
  {{#each tags}}
    <span class="label label-tag">{{short}}</span>
  {{/each}}
  <div class="brief">{{markdown brief}}</div></div>
<br />


{{#if isCustomer}}<div><label><b>Budget</b></label> <span class="budget"> ${{budget}}<i>/hr</i> ({{pricing}})</spa></div>{{/if}}

 {{#with company}}
 <dl>
  <dt>Company</dt>
  <dd>
    {{#if ../associatedWithRequest}}
      {{name}}
    {{else}}
      <a href="javascript:alert('You must be assigned to this request to see the company name')">Hidden</a>
    {{/if}}
  </dd>
  {{#if ../associatedWithRequest}}
    {{#if url}}
    <dt style="display:none">Url</dt>
    <dd style="font-weight:normal"><a href="http://{{ url }}" target="_blank">http://{{ url }}</a></dd>
    {{/if}}
  {{/if}}
  <dt style="display:none">About</dt>
  <dd>{{markdown about}}</dd>
  <dt>Contact</dt>
  <dd class="contact">
    {{#each contacts}}

    <div>
      {{#if pic }}
        <img src="{{ pic }}" />
      {{else}}
        <img src="/images/default-avatar.jpg" />
      {{/if}}
    </div>

    <dl>
      <dt>Name</dt>
      <dd>

        {{#if ../../associatedWithRequest}}
          {{fullName}}
        {{else}}
          <a href="javascript:alert('You must be assigned to this request to see the contact name')">Hidden</a>
        {{/if}}

      </dd>
      {{#if isCustomer}}
        <dt>Email</dt>
        <dd>{{email}}</dd>
        <dt>Gmail</dt>
        <dd>{{gmail}}</dd>
        <dt>Twitter</dt>
        <dd>{{twitter}}</dd>
      {{/if}}
    </dl>
    {{/each}}
  </dd>
</dl>
{{/with}}

<div style="clear:both"><label><b>Availability</b></label>
  {{markdown availability}}</div>

{{#if isCustomer}}
  {{#if hasAvailableExpert }}
  <div class="form-actions book-actions">
    <a href="#book/{{_id}}" class="btn btn-warning btn-large">Book expert(s)</a>
  </div>
  {{/if }}
{{/if}}