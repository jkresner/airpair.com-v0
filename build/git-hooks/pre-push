#!/bin/sh
killall node
brunch w -s -c config-test.coffee &
mocha test/server/all.coffee
SUCCESS=$?
if [ $SUCCESS = "0" ]
then
  mocha-phantomjs http://localhost:4444/test/index.html
  SUCCESS=$?
  mocha test/integration/cleanup.coffee
fi
killall node
exit $SUCCESS