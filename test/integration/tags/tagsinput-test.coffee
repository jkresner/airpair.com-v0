require './../../test-setup'
require './../../test-http-setup'


describe "tags autocomplete", ->

  it 'add tag hides autocomplete', ->

  it 'add tag re-uses value from autocomplete input', ->

  it 'after adding new tag, can search for new tag', ->

  it 'displays error when tag not found', ->

  it 'displays error when github repo has less than 20 stars', ->

  it 'autocomplete gives results for sting matching', ->